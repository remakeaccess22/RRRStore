<!-- resources/views/components/responsive-inventory-table.blade.php -->
<div class="overflow-x-auto">
    <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
            <tr>
                <th class="px-4 sm:px-6 py-2 sm:py-3 text-left text-xs sm:text-sm font-semibold uppercase tracking-wide text-gray-800">
                    Product
                </th>
                <th class="px-4 sm:px-6 py-2 sm:py-3 text-left text-xs sm:text-sm font-semibold uppercase tracking-wide text-gray-800">
                    Name
                </th>
                <th class="px-4 sm:px-6 py-2 sm:py-3 text-right text-xs sm:text-sm font-semibold uppercase tracking-wide text-gray-800">
                    Quantity
                </th>
                <th class="px-4 sm:px-6 py-2 sm:py-3 text-right text-xs sm:text-sm font-semibold uppercase tracking-wide text-gray-800">
                    Price
                </th>
                <th class="px-4 sm:px-6 py-2 sm:py-3 text-right text-xs sm:text-sm font-semibold uppercase tracking-wide text-gray-800">
                    Expiration
                </th>
                <th class="px-4 sm:px-6 py-2 sm:py-3 text-center text-xs sm:text-sm font-semibold uppercase tracking-wide text-gray-800">
                    Actions
                </th>
            </tr>
        </thead>
        <tbody class="divide-y divide-gray-200">
            @foreach ($data as $item)
            <tr>
                <td class="px-4 sm:px-6 py-2 sm:py-3 text-left text-gray-800">
                    {{ $item['id'] }}
                </td>
                <td class="px-4 sm:px-6 py-2 sm:py-3 text-left text-gray-800">
                    {{ $item['name'] }}
                </td>
                <td class="px-4 sm:px-6 py-2 sm:py-3 text-right text-gray-800">
                    {{ $item['instock'] }}
                </td>
                <td class="px-4 sm:px-6 py-2 sm:py-3 text-right text-gray-800">
                    {{ number_format($item['price'], 2) }}
                </td>
                <td class="px-4 sm:px-6 py-2 sm:py-3 text-right text-gray-800">
                    {{ $item['expirationDate'] }}
                </td>
                <td class="px-4 sm:px-6 py-2 sm:py-3 text-center">
                    <div class="flex justify-center space-x-2">
                        <x-action-button type="button" label="View" icon="eye" color="bg-gray-500" hoverColor="hover:bg-gray-600" />
                        <x-action-button type="button" label="Edit" icon="edit" color="bg-blue-500" hoverColor="hover:bg-blue-600" />
                        <x-action-button type="button" label="Remove" icon="trash" color="bg-red-500" hoverColor="hover:bg-red-600" />
                    </div>
                </td>
            </tr>
            @endforeach
        </tbody>
    </table>
</div>
